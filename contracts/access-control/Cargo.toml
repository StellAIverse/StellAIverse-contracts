[package]
name = "access-control"
version = "0.1.0"
edition = "2021"
description = "Role-based access control contract for StellAIverse"

[lib]
crate-type = ["cdylib", "rlib"]

[dependencies]
soroban-sdk = { workspace = true }

[dev-dependencies]
soroban-sdk = { workspace = true, features = ["testutils"] }

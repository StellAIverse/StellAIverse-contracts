[package]
name = "bridge"
version = "0.1.0"
description = "Cross-chain bridge contract for StellAIverse"
authors = ["StellAIverse Team"]
edition = "2021"

[lib]
crate-type = ["cdylib"]

[dependencies]
soroban-sdk = "21.0.0"
stellai_lib = { path = "../../lib" }
shared = { path = "../../shared" }

[dev-dependencies]
soroban-sdk = { version = "21.0.0", features = ["testutils"] }
shared = { path = "../../shared", features = ["testutils"] }

[features]
testutils = ["soroban-sdk/testutils"]
